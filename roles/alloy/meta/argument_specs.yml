---
argument_specs:
  main:
    short_description: Entry point for the alloy role.
    description:
    - Validate variables for the `alloy` role.
    options:
      alloy_service_enabled:
        description:
        - Controls `alloy_service_enabled` for the `alloy` role.
        required: false
        type: bool
      alloy_service_state:
        description:
        - Controls `alloy_service_state` for the `alloy` role.
        required: false
        type: str
      alloy_linux_install_package:
        description:
        - Controls `alloy_linux_install_package` for the `alloy` role.
        required: false
        type: bool
      alloy_windows_install_method:
        description:
        - Controls `alloy_windows_install_method` for the `alloy` role.
        required: false
        type: str
      alloy_windows_installer_url:
        description:
        - Controls `alloy_windows_installer_url` for the `alloy` role.
        required: false
        type: str
      alloy_windows_installer_path:
        description:
        - Controls `alloy_windows_installer_path` for the `alloy` role.
        required: false
        type: str
      alloy_windows_product_id:
        description:
        - Controls `alloy_windows_product_id` for the `alloy` role.
        required: false
        type: str
      alloy_windows_binary_path:
        description:
        - Controls `alloy_windows_binary_path` for the `alloy` role.
        required: false
        type: str
      alloy_config_file:
        description:
        - Controls `alloy_config_file` for the `alloy` role.
        required: false
        type: str
      alloy_config_dir:
        description:
        - Controls `alloy_config_dir` for the `alloy` role.
        required: false
        type: str
      alloy_data_dir:
        description:
        - Controls `alloy_data_dir` for the `alloy` role.
        required: false
        type: str
      alloy_user:
        description:
        - Controls `alloy_user` for the `alloy` role.
        required: false
        type: str
      alloy_group:
        description:
        - Controls `alloy_group` for the `alloy` role.
        required: false
        type: str
      alloy_log_level:
        description:
        - Controls `alloy_log_level` for the `alloy` role.
        required: false
        type: str
      alloy_validate_config:
        description:
        - Controls `alloy_validate_config` for the `alloy` role.
        required: false
        type: bool
      alloy_labels:
        description:
        - Controls `alloy_labels` for the `alloy` role.
        required: false
        type: dict
      alloy_loki_endpoint:
        description:
        - Controls `alloy_loki_endpoint` for the `alloy` role.
        required: false
        type: str
      alloy_loki_tenant_id:
        description:
        - Controls `alloy_loki_tenant_id` for the `alloy` role.
        required: false
        type: str
      alloy_loki_external_labels:
        description:
        - Controls `alloy_loki_external_labels` for the `alloy` role.
        required: false
        type: dict
      alloy_prometheus_remote_write_url:
        description:
        - Controls `alloy_prometheus_remote_write_url` for the `alloy` role.
        required: false
        type: str
      alloy_prometheus_remote_write_headers:
        description:
        - Controls `alloy_prometheus_remote_write_headers` for the `alloy` role.
        required: false
        type: dict
      alloy_prometheus_external_labels:
        description:
        - Controls `alloy_prometheus_external_labels` for the `alloy` role.
        required: false
        type: dict
      alloy_linux_metrics_enabled:
        description:
        - Controls `alloy_linux_metrics_enabled` for the `alloy` role.
        required: false
        type: bool
      alloy_linux_metrics_collectors_disabled:
        description:
        - Controls `alloy_linux_metrics_collectors_disabled` for the `alloy` role.
        required: false
        type: list
      alloy_linux_metrics_collectors_enabled:
        description:
        - Controls `alloy_linux_metrics_collectors_enabled` for the `alloy` role.
        required: false
        type: list
      alloy_linux_metrics_textfile_directory:
        description:
          - Optional directory for prometheus.exporter.unix textfile metrics on Linux.
          - Leave empty to disable the textfile collector directory block.
        required: false
        type: str
      alloy_linux_file_logs_enabled:
        description:
        - Controls `alloy_linux_file_logs_enabled` for the `alloy` role.
        required: false
        type: bool
      alloy_linux_journal_enabled:
        description:
        - Controls `alloy_linux_journal_enabled` for the `alloy` role.
        required: false
        type: bool
      alloy_linux_journal_max_age:
        description:
        - Controls `alloy_linux_journal_max_age` for the `alloy` role.
        required: false
        type: str
      alloy_linux_file_log_targets:
        description:
        - Controls `alloy_linux_file_log_targets` for the `alloy` role.
        required: false
        type: list
        elements: dict
      alloy_windows_metrics_enabled:
        description:
        - Controls `alloy_windows_metrics_enabled` for the `alloy` role.
        required: false
        type: bool
      alloy_windows_enabled_collectors:
        description:
        - Controls `alloy_windows_enabled_collectors` for the `alloy` role.
        required: false
        type: list
        elements: str
      alloy_windows_eventlog_enabled:
        description:
        - Controls `alloy_windows_eventlog_enabled` for the `alloy` role.
        required: false
        type: bool
      alloy_windows_eventlogs:
        description:
        - Controls `alloy_windows_eventlogs` for the `alloy` role.
        required: false
        type: list
        elements: dict
      alloy_windows_bookmark_dir:
        description:
        - Controls `alloy_windows_bookmark_dir` for the `alloy` role.
        required: false
        type: str
      alloy_extra_config:
        description:
        - Controls `alloy_extra_config` for the `alloy` role.
        required: false
        type: str
      alloy_firewall_enabled:
        description:
        - Controls `alloy_firewall_enabled` for the `alloy` role.
        required: false
        type: bool
      alloy_firewall_ports:
        description:
        - Controls `alloy_firewall_ports` for the `alloy` role.
        required: false
        type: list
      alloy_service_override:
        description:
        - Controls `alloy_service_override` for the `alloy` role.
        required: false
        type: dict
